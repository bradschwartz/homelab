# Pi connection
export PI_HOST=raspberrypi
export PI_USER=brad

# Tailscale OAuth credentials (create at https://login.tailscale.com/admin/settings/trust-credentials)
# Scopes needed: Devices/Core, General/Services, Keys/Auth Keys (all with tag:k8s-operator)
export TS_OAUTH_CLIENT_ID=k123456CNTRL
export TS_OAUTH_CLIENT_SECRET=tskey-client-k123456CNTRL-xxxxx

# GitHub token with repo scope for Flux bootstrap
export GITHUB_TOKEN=ghp_xxxx
export GITHUB_USER=bradschwartz
